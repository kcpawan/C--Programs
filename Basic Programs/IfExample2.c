#include <stdio.h>

int main() {

    // Number of people in the audience
    int num;
    
    printf("enter the Number of audience:");
    scanf("%d",&num);
    
    // Conditional code inside decision making statement
    if (num > 50) {
        printf("Start the show");
    }

    return 0;
}
